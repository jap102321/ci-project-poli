pipeline {
    agent any

    stages {
        stage("build"){
            steps{
                echo "project build from jenkins"
            }
        }
        stage("test"){
            steps{
                echo "testing from jenkins"
            }
        }
        stage("deploy"){
            steps{
                echo "deploy from jenkins"
            }
        }
    }
}
